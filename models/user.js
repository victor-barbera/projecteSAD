class User {
    constructor(id, name, email, password, contacts, imageSrc) {
      this.id = id;
      this.name = name;
      this.email = email;
      this.password  = password;
      this.contacts = contacts;
      this.imageSrc = imageSrc;
    }
  }
  
  export default User;